// export default function stats(links) {
//   const totalLinks = links.length;
//   const uniqueLinks = new Set(links.map((link) => link.href)).size;
//   const brokenLinks = links.filter((link) => (link.status === false)).length;

//   return {
//     totalLinks,
//     uniqueLinks,
//     brokenLinks,
//   };
// }
