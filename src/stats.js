// function stats(links) {
//   const totalLinks = links.length;
//   const uniqueLinks = [...new Set(links)];
//   // O construtor "Set" é um objeto embutido do JavaScript que representa uma
//   // coleção de elementos únicos e não repetidos. Ele é utilizado para criar um
//   // conjunto (ou conjunto de valores), que é uma coleção de valores onde cada valor
//   // pode ocorrer apenas uma vez;
// }
